# Rebuild report — 2026-09-10/11

The slim rebuild described in `AUDIT.md` §12 was executed in six local commits on `main`,
then pushed and deployed (see *Post-launch session* at the end). This report says what was built, what was deleted, where every number in the
model card comes from, what is still missing, and what only the owner can do.

## Commits

| Phase | Commit | Content |
|---|---|---|
| 0 | `66ed291` | `main` fast-forwarded to the production branch; `AUDIT.md`; rescued trainer in `ffai/_legacy/`; credential-bearing scripts deleted; ADR-0001..0003 |
| 1 | `d46a5a3` | nflverse loader + cache, rules scoring (exact reconciliation), as-of feature module, data contracts, 21 tests, committed CI fixture |
| 2 | `c3a5a2e` | RF/XGB trainer, GMM tiers, evaluator (causal baseline, rolling origin, artifact v2.0), drift, registry, model card; artifacts `20260911-asof_v1-d333de20`, `20260911-tiers_prevseason_v1-2024`, `eval-20260911-…-rf` |
| 3 | `012c22c`, `c0a47cc` | FastAPI server reading the manifest, week scoring, `artifacts/predictions/2025/week_01.json`, Dockerfile, API tests |
| 4 | `d7b7f80` | 177 legacy files deleted; frontend rewired to the API with no hardcoded metrics; README, ARCHITECTURE, DECISIONS, DATA_SOURCES, compliance, case study |
| 5 | `dfc383d` | CI workflow, weekly workflow, `ffai/pipeline/weekly.py`, robust drift rule (ADR-0010), policy tests |

Tracked files: 240 (audit) → 305 (after the production-branch merge) → **128**.

## What was built

| Area | Module(s) | Tests |
|---|---|---|
| Data | `ffai/data/nflverse.py` (nflreadpy 0.1.5, dated parquet cache), `ffai/data/contracts.py` | `test_contracts.py` (6) |
| Scoring | `ffai/scoring.py` — standard / half / PPR from explicit rules; `reconcile()` is exact (max abs diff 0.0000) on all 34,293 rows 2019–2024 | `test_scoring_reconciliation.py` (3) |
| Features | `ffai/features/asof.py` — the single as-of builder, 65 features (QB 44 / RB 40 / WR 27 / TE 27), groupby-safe shifted windows, future-week stubs | `test_asof_no_leakage.py` (3: 200 independent recomputations, perturbation of target/future weeks, future-week stubs), `test_grain.py` (6) |
| Models | `ffai/models/train.py` (RF champion, XGB challenger, sklearn Pipelines with `feature_names_in_`, validation-residual intervals, bucketed drift references), `ffai/models/tiers.py` (scaler → PCA ≥ 90% → GMM by BIC on prior-season aggregates), `ffai/models/registry.py` (manifest, `should_promote`) | `test_registry.py` (3) |
| Evaluation | `ffai/eval/evaluator.py` (forward holdout, causal trailing-mean baseline seeded with history, rolling-origin folds, artifact with sha256/commit/definitions), `ffai/eval/drift.py`, `ffai/eval/model_card.py` | `test_evaluator.py` (8), `test_drift.py` (9) |
| Serving | `ffai/serve/app.py`, `schemas.py`; `Dockerfile` (python:3.11-slim, non-root, :7860); `requirements-api.txt` (8 packages) | `test_api.py` (6) |
| Weekly job | `ffai/pipeline/score.py`, `ffai/pipeline/weekly.py`, `scripts/run_weekly.py`, `.github/workflows/weekly.yml` | `test_weekly_policy.py` (5) |
| CI | `.github/workflows/ci.yml` | — |
| Frontend | `frontend-next/` — 14 routes, every figure fetched from the API, no auth/payment/fixtures | `npm run build` + `npm run lint` clean |

Suite: **49 tests pass** offline (committed 40-player fixture) and with `FFAI_TEST_DATA=full`.

## What was deleted

`backend/` (119 tracked files plus the untracked `backend/scripts/**`, `backend/models/archive/**`,
`backend/docs/archive/**`), `infrastructure/` (compose, empty Dockerfile, nginx, terraform stub),
40 legacy `scripts/` files (demos on synthetic data, deploy scripts, Sleeper ingest, smoke tests),
root `models/` (synthetic-data NNs, hand-written `feature_importance.json`), `vercel.json`,
`deploy-to-railway.sh`, the old `docker-compose.yml` and `Makefile`, seven stale docs, the
Streamlit references, `.ipynb_checkpoints/`, `ssl/`, and in the frontend: `auth/*`, `dashboard`,
`pricing`, `contact`, `features`, Clerk, Stripe, the `predictions_2024.json` / `tiers_2024.json`
fixtures, mock fallbacks, and every hardcoded metric string.

Kept for reference: `AUDIT.md`, `ffai/_legacy/train_production_ml_models.py`,
`analytics/sql/risk_strategy.sql` (design sketch), the remote `codex/*` branches.

## Every number in the model card and where it comes from

`docs/MODEL_CARD.md` is generated by `ffai/eval/model_card.py`; the table below is the trace.

| Figure (model card) | Artifact key |
|---|---|
| Model version, feature version, target, data library, input rows, input sha256, data through, split seasons, trained at, training commit, library versions, interval method | `artifacts/models/20260911-asof_v1-d333de20/metadata.json`: `model_version, feature_version, target, data_library, input_rows, input_sha256, data_through, seasons, trained_at_utc, code_commit, sklearn_version, xgboost_version, interval_method` |
| Feature counts (65; 44/40/27/27) | `metadata.json: positions[pos].n_features`, `positions[pos].features` |
| Frozen-test table (All / QB / RB / WR / TE: n, MAE, median AE, RMSE, within ±3, within ±5, baseline MAE, baseline within ±3) | `artifacts/eval/eval-20260911-20260911-asof_v1-d333de20-rf.json`: `metrics.*`, `cohorts[pos].*`, `baseline.*`, `cohorts[pos].baseline.*` |
| Metric definitions and baseline definition | same file: `metric_definitions.*` |
| Evaluation commit, eval id, input sha256 | same file: `code_commit`, `eval_id`, `input.sha256` |
| Candidate comparison (RF/XGB validation and test MAE, position-mean baseline) and sample sizes | `metadata.json: positions[pos].candidates[rf|xgb].val_mae / test_mae`, `positions[pos].baseline_position_mean.test_mae`, `positions[pos].n_train / n_val / n_test` |
| Rolling-origin mean MAE, baseline, per-week folds | eval artifact: `rolling_origin.mean_mae`, `rolling_origin.mean_baseline_mae`, `rolling_origin.folds[]` |
| Tiers table (components, PCA dims, players, Spearman, within-band) | `artifacts/tiers/20260911-tiers_prevseason_v1-2024/metadata.json`: `positions[pos].n_components / pca_components / n_players`, `evaluation.positions[pos].spearman / within_band_rate` |

Headline values at the time of writing (read from those keys, not typed): frozen 2024 test
n = 5,747, MAE 4.5512, within ±3 = 44.9 %, causal-baseline MAE 4.8589; rolling-origin mean MAE
4.5221 vs baseline 5.2697 over 17 folds; RF is champion at every position. The legacy metadata
recorded 6.17 / 4.92 / 4.99 / 3.88 on a 31,000-row pull; the rebuilt nflverse release has 34,293
rows and gives 6.13 / 4.50 / 4.61 / 3.64. The audit's "91 % within ±3" had no source; the
measured value is 44.9 %.

## Departures from the brief

1. **Drift HOLD rule (ADR-0010).** The specified "any top-10 feature PSI > 0.25 → HOLD" fired on
   47 of 60 normal four-week windows, including training-season windows. Implemented instead:
   median monitored PSI > 0.25 (with ≥ 3 monitored features) or ≥ 2 features > 0.50 → HOLD; any
   feature > 0.25 → WARN; four-week window of played rows; week-of-season-matched reference;
   structural and season-to-date features excluded. Thresholds are constants in
   `ffai/eval/drift.py` and every run log records the per-position medians and flagged features.
2. **Docker image size not measured locally.** Docker Desktop fails to launch on this machine
   (launchd spawn error). The CI `docker` job builds the image and prints `docker image inspect
   --format '{{.Size}}'`; estimate from wheel sizes is 400–450 MB uncompressed.
3. **`.env.example` files.** The brief asked that `git ls-files` show no env files; the two
   placeholder-only `*.env.example` files are tracked deliberately (an explicit `!.env.example`
   negation), and no real env file is tracked.
4. **Local `xgboost` needs a vendored `libomp`** (Homebrew has no bottle on this Intel Mac); the
   venv carries a copy from the owner's conda env. Linux CI wheels are unaffected.
5. **Editable install quirk.** In this uv-created venv the editable `.pth` is not honoured when
   Python runs a file, so the five `scripts/*.py` insert the repository root into `sys.path`
   themselves. CI installs the package normally.

## Remaining limitations

* Features are the player's own prior production only; no opponent, injury, weather, depth
  chart, or market inputs. Players without a prior stat row get no prediction; rookies get no tier.
* Intervals are validation-residual quantiles; their empirical coverage is not yet tracked weekly.
* The policy layer (decision score, threshold simulator, decision marts) is designed in the
  evaluator and the SQL sketch but not implemented; `policy_sweep` is empty.
* `GET /players/{id}` history comes only from the frozen 2024 test predictions and from weekly
  prediction files that have had actuals attached; earlier seasons are not served.
* The QB tier model chose 10 BIC components for 60 players (within-band 29.6 %); recorded, not tuned.
* Model retraining is manual (`make train && make evaluate`); the weekly job scores, evaluates,
  and promotes but never retrains.
* Current-season detection relies on nflverse schedules; if a new season's stats are not yet
  published the freshness contract fails and the job HOLDs (by design).

## Owner TODO (only you can do these)

1. **Rotate the Supabase password** for `db.ypxqifnqokwxrvqqtsgc.supabase.co` (it sits in public
   git history since 2025-07-31) — or delete that database.
2. **Rotate the OpenAI and Anthropic keys** that were in `backend/.env.local` (now
   `.env.backend.local`, git-ignored), then delete `.env.backend` and `.env.backend.local`.
3. Decide whether to **purge history** (`git filter-repo`) for the two deleted credential scripts;
   rotation alone makes the leaked value harmless.
4. **Push `main`** and delete/archive the `codex/*` remote branches once you are satisfied.
5. **Enable GitHub Actions** on the repository (Settings → Actions) and allow workflows to write
   (`contents: write`, `issues: write` are declared in `weekly.yml`).
6. **Create the Hugging Face Space** (Docker SDK, port 7860) from this `Dockerfile`, or any
   container host; note the URL.
7. **Set `NEXT_PUBLIC_API_URL`** on Vercel to that URL and redeploy `frontend-next` from `main`
   (the live site currently builds from the old `codex/*` branch).
8. Optionally trigger `weekly.yml` manually once to seed the current season
   (`Run workflow` → leave season/week blank).
9. Re-check the nflverse data terms before redistributing derived data (`docs/DATA_SOURCES.md`).


## Post-launch session (2026-09-11)

State on entry: `main` at `8e22a47`, in sync with `origin/main`; API live on the Hugging Face
Space `cbratkovics/fantasy-football-ai` (populated with `hf upload`); frontend on Vercel
(`fantasy-football-ai.vercel.app`, `www.winmyleague.ai`); Railway and Supabase deleted.

| Phase | Commit | Content |
|---|---|---|
| A | `03658fc` | 2025 out-of-sample evaluation of the frozen champion (`ffai/eval/oos.py`, `oos_predictions_2025.csv`, `eval-…-rf-oos2025.json`); `manifest.evaluations` with `kind`; repo-relative `input.path` (2024 artifact regenerated, metrics asserted identical); model card with a second table and a delta paragraph; `GET /`, `GET /performance` → list, `GET /performance/{eval_id}`; frontend evaluation selector |
| B | `636d79e` | weekly job resolves 2026 week 1 from schedules, drops the target week's partial rows before contracts, names the missing (season, week) on a freshness HOLD; first real 2026 run (`predictions/2026/week_01.json`, 610 players, PUBLISH); `rolling_2026.json` created empty; `predictions/2025/week_01.json` removed (ADR-0011); Space mirror step in `weekly.yml` + manual `deploy-space` job in `ci.yml` |
| C | `9e64c6d` | GMM component cap `n_players // 8` (ADR-0012); new `tiers_version …-2024-mpc8`; before/after comparison with a keep-previous rule; QB 10 → 4 components |
| D | see git log | docs, tests (62), Space re-upload |

### Numbers added to the model card (artifact keys)

| Figure | Key |
|---|---|
| 2025 out-of-sample table (n, MAE, median AE, RMSE, within ±3/±5, baseline) for All and per position | `artifacts/eval/eval-20260911-20260911-asof_v1-d333de20-rf-oos2025.json: metrics.*, cohorts[pos].*, baseline.*` |
| 2025 rolling-origin means and folds | same file: `rolling_origin.*` |
| 2024 → 2025 delta paragraph | computed at render time from the two artifacts' `metrics`, `baseline`, `cohorts`, `rolling_origin` (no stored value) |
| Tier before/after table | `artifacts/tiers/20260911-tiers_prevseason_v1-2024-mpc8/metadata.json: comparison_to_previous` |

Headline values read from those keys at the time of writing: 2025 out of sample n = 5,914, MAE
4.4909, within ±3 = 46.0 %, causal baseline MAE 4.8046 (2024 frozen: 5,747 / 4.5512 / 44.9 % /
4.8589). The aggregate improvement (−0.060) is of the same size as the baseline's own move
(−0.054); QB got harder for both (+0.50 model, +0.39 baseline). Not a leakage signal.

### Verified facts about the data

nflverse has the complete 2025 regular season (18 weeks, 6,037 QB/RB/WR/TE rows, columns identical
to 2024, scoring reconciliation exact) and a 2026 schedule whose first game day is 2026-09-09; on
2026-09-11 two games had results and 23 partial week-1 stat rows existed.

### Departures / judgment calls

* The brief said 2026 week-1 games were Sept 4–8; nflverse's schedule says Sept 9 onward. The
  job trusts the schedule.
* Partial rows of the week being scored are dropped before the contracts (otherwise every
  in-progress week would HOLD on the row-count band).
* The tier cap changed only QB; its Spearman fell while within-band rose, so the both-worse rule
  kept the new tiers. Both numbers are in the card.

### Owner TODO

1. Push `main` (four new commits after `8e22a47`).
2. Add the repository secret `HF_TOKEN` (Hugging Face token, **write** scope) so `weekly.yml`
   can mirror the Space after each PUBLISH/PROMOTE and `ci.yml`'s `deploy-space` works.
3. Trigger `weekly.yml` once by hand (`Run workflow`, leave inputs blank) to confirm the Actions
   path end to end; next Tuesday's run will HOLD until nflverse publishes complete 2026 week 1
   stats and then publish week 2.
4. Check the Vercel deploy of `main` (the `/performance` page now needs the list-shaped
   `/performance`, which the Space already serves).
