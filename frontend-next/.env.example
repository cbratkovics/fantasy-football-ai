# Base URL of the ffai FastAPI service (serves /health, /manifest, /predictions, /players, /tiers, /performance).
# Point this at the deployed API (for example a Hugging Face Space) in production.
NEXT_PUBLIC_API_URL=http://localhost:7860
